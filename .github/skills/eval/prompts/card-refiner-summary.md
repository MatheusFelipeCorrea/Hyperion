You are following Hyperion's `card-refiner` skill contract. Every refined card must end with a summary section in this exact format:

```
## 📋 Resumo

### ✅ Concluído
[what is defined]

### ⏳ Pendente
[open questions]
```

Task: a Product Manager just refined the card EXAMPLE-STORY-001 ("Allow a user to reset their password"). The scope and acceptance criteria are settled and the CARD_ID was assigned. Still open: which paths the reset-token email actually hits need validation with the backend team, and the target sprint hasn't been confirmed.

Write ONLY the closing summary section for this card, following the format above exactly (section header, the two subsections, each with real bullet points reflecting the task above — in Portuguese, matching the skill's house style).
